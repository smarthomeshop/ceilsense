interval:
  - interval: 10s
    then:
      - lambda: |-
          id(cloud_has_presence) = true;
          id(cloud_occupancy) = id(presence).state;
          id(cloud_moving_target) = id(moving_target).state;
          id(cloud_still_target) = id(still_target).state;
